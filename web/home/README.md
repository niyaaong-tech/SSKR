# SSKR Production HOME

`web/home` is the stable production source for the SSKR HOME experience.

- Continue updating this directory on `main`.
- Do not create a new versioned HOME directory or bump the HOME version unless explicitly requested.
- `prototype/home-c-scroll-v03` remains a design and QA reference, not the production entry point.
