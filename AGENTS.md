# SSKR 저장소 작업 지침

이 지침은 저장소 전체에 적용한다. 이 저장소는 자료 보관소가 아니라 현재 웹 결과물과 이를 유지하는 데 필요한 최소 소스만 관리한다.

## 1. 기본 원칙

- `main`에는 현재 선택된 결과물만 남긴다.
- Git 이력을 백업으로 사용한다. 별도 백업 폴더, 복제 저장소, 날짜별 사본, `v1`, `v2`, `final-final` 형식의 파일과 폴더를 만들지 않는다.
- 백업 브랜치나 태그는 사용자가 명시적으로 요청한 경우에만 만든다. 자동으로 보존용 브랜치를 늘리지 않는다.
- 문서의 정본은 Notion이다. 저장소에는 `README.md`, `AGENTS.md`, 실행·배포에 반드시 필요한 기술 문서만 둔다.

## 2. 이름 규칙

- 파일과 폴더 이름은 버전이나 작업자의 감상이 아니라 역할을 설명해야 한다.
- `final`, `final2`, `real-final`, `latest`, `new`, `old`, `backup`, `copy`, `fix` 같은 상태 표현을 이름에 사용하지 않는다.
- `v1`, `v2`, 날짜, 수정 횟수는 동시에 유지해야 하는 공개 규격이 아닌 한 이름에 붙이지 않는다. 변경 이력은 Git이 관리한다.
- 정식 경로는 `home`, `journey`, `shared`, `source`, `build`처럼 용도가 바로 드러나는 안정된 이름을 사용한다.
- 결과가 교체되어도 정식 경로와 파일명은 유지하고 내용만 갱신한다.

## 3. 자산 단일 원본 원칙

- 동일한 자산은 원칙적으로 한 위치에서만 관리한다.
- 새 자산을 추가하기 전에 파일명 검색과 해시 비교로 기존 사본이 있는지 확인한다.
- 여러 페이지가 같은 자산을 사용하면 공용 경로 하나를 사용한다. 페이지별 폴더에 수동 복사본을 만들지 않는다.
- 편집 원본과 웹 결과물이 모두 필요한 경우 각각의 역할과 생성 관계를 명확히 한다. 재생성에 필요하지 않은 편집 중간본은 남기지 않는다.
- 완성 결과물이 확정되면 대체된 이미지, 3D 모델, 데이터, 캡처, 생성 스크립트와 참조를 같은 작업에서 제거한다.

## 4. 프로토타입과 테스트

- 프로토타입, QA 캡처, 다운로드 파일, 변환 결과, 실험용 출력은 저장소 밖의 임시 폴더에서 만든다.
- 테스트 결과를 검토한 뒤 선택된 결과물만 정식 경로로 승격한다.
- 작업 종료 전에 임시 폴더와 실패한 후보를 삭제한다. 비교용이라는 이유로 여러 버전을 계속 누적하지 않는다.
- 장기간 유지할 필요가 생긴 프로토타입만 사용자의 명시적 승인 후 저장소에 포함한다.

## 5. 금지되는 중간 자산

- Base64 청크, `part00` 형태의 분할 파일, 압축 조각, 업로드 우회용 파일을 커밋하지 않는다.
- 대용량 원본이 Git에 적합하지 않으면 Git LFS, 릴리스 자산 또는 외부 스토리지를 사용한다.
- `output`, `dist`, 스크린샷, 브라우저 다운로드, 캐시와 자동 생성 파일은 최종 배포에 직접 필요한 경우가 아니면 커밋하지 않는다.
- 임시 파일을 보존하기 위해 GitHub Actions나 복원용 워크플로를 추가하지 않는다.

## 6. 도구와 생성 파이프라인

- `tools/`는 현재 결과를 깨끗한 checkout에서 재현하는 데 필요한 현행 도구만 둔다. 도구 폴더를 코드 버전 보관소로 사용하지 않는다.
- 기능을 수정할 때 `tool-v2.py`, `tool-v3.py`, `tool-fix.py`를 새로 만들지 말고 역할이 같은 기존 도구를 수정한다. 이전 구현은 Git 이력에서 확인한다.
- 한 버전의 출력을 다음 버전 스크립트가 문자열 치환으로 수정하는 연쇄 패치 방식을 금지한다.
- 빌드는 하나의 명시적인 소스와 하나의 진입점에서 최종 결과를 생성해야 한다. 전체 결과를 만드는 코드와 일부만 덧대는 코드가 혼재하지 않도록 한다.
- 일회성 마이그레이션이나 변환 스크립트는 임시 폴더에서 실행하고 결과를 검증한 뒤 삭제한다. 반복 실행할 운영 도구만 저장소에 남긴다.
- 각 현행 도구는 입력, 출력, 실행 명령과 필요 이유가 분명해야 하며 누락된 과거 자산이나 다른 버전 스크립트에 의존해서는 안 된다.
- 워크플로는 긴 버전별 스크립트 목록을 실행하지 않는다. `build`, `test`, `deploy` 역할별 단일 명령을 호출한다.
- 결과를 승격할 때 더 이상 호출되지 않는 도구, 실패한 실험 도구와 이를 참조하는 워크플로를 같은 변경에서 제거한다.

## 7. 교체와 정리 방식

- 새 버전은 기존 정식 경로를 교체하고 Git 커밋으로 이력을 남긴다. 버전별 폴더를 병렬로 유지하지 않는다.
- 파일이나 자산을 제거할 때는 `rg`로 모든 참조를 확인한다.
- 제거된 입력을 참조하는 워크플로, 스크립트, 설정과 문서도 함께 수정하거나 제거한다.
- 사용 여부가 불명확한 대용량 자산은 파일 형식, 참조 위치, 해시 중복과 현재 배포 사용 여부를 확인한 뒤 판단한다.
- 사용자 작업과 미추적 파일은 임의로 삭제하지 않는다.

## 8. 완료 조건

작업은 다음을 모두 만족해야 완료로 본다.

1. 현재 선택된 결과물과 필수 편집 원본만 남아 있다.
2. 구버전, 실패한 테스트, 청크, 중복 사본과 임시 출력이 없다.
3. 삭제된 경로를 참조하는 코드, 워크플로와 설정이 없다.
4. `git status`에 의도하지 않은 변경이나 미추적 결과물이 없다.
5. 공개 경로 `/`와 `/journey` 등 영향받는 화면을 확인했다.
6. 추가·유지·삭제한 대용량 자산을 작업 결과에 명시했다.

## 9. 현재 정식 위치

- HOME 배포 결과: `web/home/`
- Participate 배포 결과: `web/participate/`
- Journey Presentation 코드와 배포 결과: `web/journey-presentation/`
- 경로 설정: `vercel.json`

이 목록이 변경되면 이전 경로를 남겨두지 말고 이 지침과 `README.md`를 함께 갱신한다.
